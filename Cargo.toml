[package]
name = "ion-rust"
version = "0.1.0"
edition = "2018"

[dependencies]
bigdecimal = "~0.1"
bytes = "~0.4"
chrono = "~0.4"
delegate = "0.4.2"
failure = "~0.1"
failure_derive = "~0.1"

[dev-dependencies]
# These are used by the ion_grep example
crossbeam = "~0.7"
memmap = "~0.7"
num_cpus = "~1.13"
regex = "~1"
# Used by ion-tests integration
walkdir = "~2.3"

[profile.release]
lto = true
codegen-units = 1
